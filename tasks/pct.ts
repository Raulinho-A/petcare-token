import { task } from "hardhat/config";

// task("pct:balance", "Muestra el balance PCT de una address")
//   .addParam("account", "Address a consultar")
//   .setAction(async ({ account }, { ethers }) => {
//     const addr = "0xc88eE28E5a33407037CA1E589E15678C5b91DfA5";
//     const token = await ethers.getContractAt("PetCareToken", addr);
//     const bal = await token.balanceOf(account);
//     console.log("Balance:", ethers.formatUnits(bal, 18), "PCT");
//   });
